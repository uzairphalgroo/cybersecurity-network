{
  "buildCommand": "cd \"cybersecurity projects/ && npm install && npm run build",
  "outputDirectory": "cybersecurity projects",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
