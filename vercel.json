{
  "buildCommand": "cd audithound/frontend && npm install && npm run build",
  "outputDirectory": "audithound/frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
