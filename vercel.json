{
  "buildCommand": "cd sentinara/frontend && npm install && npm run build",
  "outputDirectory": "sentinara/frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
